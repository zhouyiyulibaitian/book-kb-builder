---
name: book-kb-builder
description: 把上传的图书（尤其设计类）自动读进一个分层知识库——探测格式与文字层、按类别归档为 知识库/类别/书名/（电子版 + 阅读总结 txt）、为每本书写一份带页锚的总结并做防编造校验。当用户说「把这本书加进知识库」「帮我建一个图书知识库」「把这些书分类整理/归档」「读一下这本书并生成总结」「我上传了书，帮我入库」，或要求整理、补写、更新已有图书知识库时使用。内容必须有原书页锚、禁止编造；联网补充只在专门章节并标注来源。Use when the user uploads books (PDF/EPUB) and wants them read, classified into a knowledge base, and summarized with page-anchored notes.
---

# book-kb-builder：把图书读进知识库

把一本（或一批）电子书，读成一份**每个结论都能回到原书页码**的总结，
并按类别归档成一个可长期检索的分层知识库。

核心不是"生成一段像样的书评"，而是**产出可核对的证据**：
以后你或别人翻这份总结时，看到 `(p.188)` 就能翻到原书第 188 页验证。

## 本机 DSH 适配规则（实测约束）

> 本节只记录**本机环境**约束与"校验器变严之后该怎么写"。
> 本 skill 的两个假通过已在本机副本中修复，因此引文纪律与上游正文不同：
> 校验器现在**会**逐段核对省略号节选、也**会**比对引文实际所在页。

**调用方式**

```bash
<py> = C:\Users\zhouyi\.agents\skills\book-kb-builder\.venv\Scripts\python.exe
<skill> = C:\Users\zhouyi\.agents\skills\book-kb-builder
命令：<py> "<skill>\scripts\booktool.py|kbtool.py|verify.py" ...
```

**校验器已变严，写作时要跟着变**

1. **省略号节选会被逐段核对。** 以前 `……` 只校验第一段，"真实句子 + 编造尾巴"能全过；
   现在每一段都必须在原文中找到，否则报 `引文逐字核对 fail`。
   → 用 `……` 节选是允许的，但**每一段都必须逐字来自原文**。
2. **引文的页锚会被比对。** 以前只查"这句话存在"，现在还会查"它是不是真在标注的那一页"，
   错页报 `引文页锚 fail`（提示会告诉你实际出现在哪一页）。
   → 引文后面紧跟 `(p.N)`，N 必须是这句话**实际出现**的那一页。
3. **引文缺页锚现在会被点名。** 只警告不判错，但会列出具体行号，请补上。

**两条写作纪律（仍然适用）**

- **引文核对报错时先怀疑字符形近，不要直接删引文**：本机 PyMuPDF 用 china-s 字体提取时，
  `…`(U+2026) 可能变成 `⋯`(U+22EF)，逐字比对会误报。回原页面图确认后再改。
- **扫描版的语料优先用 `pdf-toolkit` 的 `ocr --sidecar` 产出**：用 pdf-toolkit 的 venv 加 OCR
  文字层并同时产出 `<书名>.pages.txt`，拿它当 `verify.py --source`。
  （`pdf-toolkit to-ai` 现在也会正确标注 `source` 了，但 sidecar 仍是最直接的语料。）

**本机环境约束**

- **分类路径只用全角 `＞` 作分隔符**：本版只认这一个字符。
  重复入库的"跳过"是静默的（同名不同大小会拒绝并要求 `--force`），入库后用 `kbtool.py list` 确认册数。
- **更新本 skill 不要用 `git clone`**：本机 git/curl 的 HTTPS 不可用
  （`schannel SEC_E_NO_CREDENTIALS`），改用 `node -e "fetch(...)"` 拉 zip 解压覆盖。
- 本 skill 不需要网络，也不碰 `tempfile`，沙箱的目录权限坑与它无关。

**已修复的缺陷（本机副本已改，上游待 PR）**

| 原缺陷 | 修复后行为 |
|---|---|
| 省略号夹带：只校验 `……` 前的第一段 | 逐段核对，编造片段报"在原文中找不到" |
| 不比对引文实际所在页 | 新增"引文页锚"检查，标注页与定位页不符即 fail |

**实测基线（可拿来对照自己的结果）**

| 场景 | 实测结果 |
|---|---|
| 干净报告（真实引文 + 正确页锚） | exit 0，引文类检查无异常 |
| 「真实句 + `……` + 编造尾巴」 | exit 1，报"在原文中找不到" |
| 真实引文标错页（实际 p.1 标 p.3） | exit 1，报"标注 p.3，但这段引文实际出现在 p.1" |
| 纯编造引文 | exit 1，报"在原文中找不到" |
| 引文缺页锚 | 警告「补上 (p.N)」，不判错 |
| 50 条命令行 / 知识库分层 / 重复入库 | 全部符合预期，中文文件名无乱码 |
## 产出物

```
知识库/                                   ← 知识库根（默认 ./知识库）
├─ _索引.md                               ← 根索引：类别地图 + 每本书一句话
├─ <类别>/
│  ├─ _索引.md                            ← 类别索引
│  └─ <图书名称>/
│     ├─ <原电子版文件>                    ← 原件，原样保留
│     └─ <图书名称>_阅读总结.txt           ← 生成的总结（本 skill 的主要产物）
└─ _全文提取/<类别>/<图书名称>.pages.txt   ← 带 [[p.N]] 页锚的全文（有文字层时才有）
```

`_索引.md` 与 `_全文提取/` 是自动维护的辅助物：前者让后续检索能"先看地图再下钻"，
后者让总结里的引文可以被机器复核。书目目录本身严格保持用户要求的三级结构。

## 四条不可退让的原则

1. **无出处不落笔。** 总结里每个实质性判断都要带页锚 `(p.N)` 或 `(p.N·图)`。
   写不出页锚的句子，说明它不在原书里，删掉。
2. **不替作者补全。** 原书没写的，写「本书未涉及……」而不是用常识填。
   这是本 skill 最容易犯也最致命的错误。
3. **联网内容与原文物理隔离。** 只能出现在「联网补充」一节，且四要素齐全
   （结论 + 网址 + 检索日期 + 可信度）。正文里出现网址即判不通过。
4. **覆盖度诚实。** 读了多少写多少。低覆盖度不是错误，假称读全了才是。

详细写作纪律见 `references/summary-template.md`。

## 工作流程

### 第 0 步：确认输入

- **书在哪里**：用户给了路径就用；没给就先在对话上下文、当前工作目录里找
  最近出现的 pdf/epub 文件，再找 `~/Downloads`、`~/Desktop`。
  有多个候选且无法判断时，列出候选让用户选，别猜。
- **知识库在哪**：用户指定了就用；否则用当前工作目录下的 `知识库/`。
  已存在就先 `kbtool.py list` 看现状。
- **能不能联网**：默认允许，但只用于书目核实、作者背景、学术接受、术语对照四类，
  见 `references/web-search-policy.md`。用户说不要联网就不联网。

多本书就逐本走完流程，最后统一刷新索引（`build` 每次都会自动刷新，不必手动）。

### 第 1 步：探测这本书（别跳过）

```bash
<本skill目录>/.venv/Scripts/python.exe scripts/booktool.py probe "<书>" --work .bookwork
```

（macOS / Linux 用 `.venv/bin/python`。venv 不存在就先按 `references/extraction.md`
最后一节创建并装 PyMuPDF——装包前先告诉用户。）

`probe` 会给出**文字层判定**，这决定走哪条路，务必看清：

- `text-rich` → **文本路线**：`search` 定位 + `text` 精读，引文可被机器核对。
- `scanned` / `text-garbled` → **视觉路线**：渲染页面图片来读。
  **设计类老书、图册、影印本大量是扫描版**，这不是罕见情况。
- `text-partial` → 两条路都用：正文读文本，图版页读图片。

路线细节、页码校准（扫描版必做）、联系表用法见 `references/extraction.md`。

### 第 2 步：定类别

先看知识库已有什么类别，再决定：

```bash
<py> scripts/kbtool.py list --kb-root 知识库
<py> scripts/kbtool.py plan --kb-root 知识库 --category "<类别>" --title "<书名>"
```

分类依据必须来自书本身（书名、目录、前言里的自我定位、丛书信息），
**已有类别能装下就绝不新建**。完整类别清单与判定规则见 `references/taxonomy.md`。

### 第 3 步：读

按 `references/reading-protocol.md` 的四轮走：**测绘 → 精读骨架 → 抽样正文 → 回填自检**。

关键点：

- **目录、前言/序/导论、结语/结论、后记，必须 100% 读完。**
  「本书研究的问题」和「研究方向」几乎都写在这几处，是全书信息密度最高的地方。
- 正文按章抽样：每章读开篇 2-4 页 + 章末 1-2 页，用 `search` 精确命中关键段落。
- 扫描版先做**页码校准**（渲染一页正文，看印刷页码与 PDF 页号的差值），
  否则你写的页锚会整体偏移。
- 预算用尽时，**降低覆盖度声明，而不是降低真实性**。

阅读时随手记页锚。看到值得引用的句子，立刻记下页码与原文，别等到写的时候凭印象。

### 第 4 步：写总结

照 `references/summary-template.md` 的结构写，存成
`<知识库>/.bookwork/<书名>_阅读总结.txt`（落库时会被复制进书目目录）。

最容易出问题的是这几节，写完自查一遍：

- **本书研究的问题**：优先引用作者在前言/绪论里的原话，别自己概括成一句空话。
- **原文摘录**：这是最容易被抽查的部分，必须逐字。
  省略中间用 `……`，脚本会分段核对。
- **覆盖度**：写明「精读 X 页 / 全书 Y 页」，并列出哪些部分没读、因此哪些问题答不了。

### 第 5 步：联网补充（可选）

只在确有帮助时查（书目核实、作者背景、学术接受、术语对照），
写进「联网补充」一节并标全四要素。没用联网就保留该节写「本总结未使用联网检索内容」。
分级与格式见 `references/web-search-policy.md`。

### 第 6 步：校验（闸门，不可跳过）

```bash
<py> scripts/verify.py --summary "<总结.txt>" --source .bookwork/<书名>.pages.txt
```

（扫描版没有 `--source`，relax：脚本会跳过引文机械核对，但页锚范围、结构完整性、
联网隔离、覆盖度声明仍然照查。）

它会检查：必备章节是否齐全、页锚是否落在书内、**引文能否在原文中逐字找到**、
联网内容是否泄漏到正文、覆盖度是否写清、有没有带数字却没有出处的句子。

**凡是不通过的，修完再重跑，直到通过。** 引文对不上就是改回原文或删掉，
绝不要为了让校验通过而把引号拆掉——那等于把"防编造"改成"防检测"。

扫描版没有文本可比对，这道闸门挡不住编造，所以视觉路线的自觉得自己守住：
每条结论都带 `(p.N·图)`，并确保那张页面图你真的看过。

### 第 7 步：落库

```bash
<py> scripts/kbtool.py build --kb-root 知识库 \
    --category "<类别>" --title "<书名>" \
    --book "<电子版路径>" --summary "<总结.txt>" \
    --extract .bookwork/<书名>.pages.txt      # 有文字层时才带这个参数
```

`build` 会建好 `类别/书名/` 目录、复制原件与总结、刷新两级索引。
默认**复制**电子版（不动用户原件）；用户明确说要挪走才加 `--move`。

落库成功后再删工作目录，里面有渲染出来的页面图，可能占几百 MB：

```bash
rm -rf .bookwork
```

（`verify.py` 以后要复检时，用知识库里的 `_全文提取/.../<书名>.pages.txt` 当 `--source` 即可。）

### 第 8 步：汇报

告诉用户这几件事，缺一不可：

- 归档到了哪个路径、归入哪个类别、分类依据是什么（方便用户纠正）
- 阅读覆盖度：精读多少页 / 全书多少页，哪些没读
- `verify.py` 的结论（通过 / 有几项待人工确认）
- 哪些内容是联网补充的（如果有）
- 你自己的不确定之处

覆盖度低就直说。用户据此决定要不要让你补读某几章。

## 工具速查

脚本在本 skill 目录的 `scripts/` 下，用本 skill 自带 venv 里的 python 运行。

| 目的 | 命令 |
|---|---|
| 探测格式/页数/文字层 | `booktool.py probe <书> --work .bookwork` |
| 拿章节页范围 | `booktool.py outline <书> --chapters` |
| 关键词定位（含页号） | `booktool.py search <书> --pattern 词` |
| 按页读文本 | `booktool.py text <书> --pages 14-20` |
| 渲染页面成图 | `booktool.py render <书> --pages 5-8 --offset 13` |
| 多页缩略图联系表 | `booktool.py contact <书> --pages 9-60` |
| 总结校验 | `verify.py --summary S.txt --source P.txt` |
| 列知识库现有书 | `kbtool.py list --kb-root 知识库` |
| 预演落库路径 | `kbtool.py plan --kb-root 知识库 --category C --title T` |
| 落库并刷新索引 | `kbtool.py build ... ` |
| 只刷新索引 | `kbtool.py index --kb-root 知识库` |

## 遇到这些情况怎么办

- **书是扫描版，抽出来是空的**：正常，走视觉路线。先 `outline --chapters`，
  没有大纲就 `contact` 找目录页。别反复尝试抽取。
- **没有书签大纲、也没有文字层**：用 `contact` 生成前 60 页的联系表，
  目录通常在前言之后，一眼能看出来；再 `render` 高清读目录页。
- **书是 EPUB 且有 DRM**：解出来是空的。告诉用户这个文件受保护，
  请换无 DRM 版本，不要硬读。
- **渲染需要装 PyMuPDF**：装第三方包会改动用户环境，**先说明用途征求同意**，
  并优先装进本 skill 的 `.venv/`，不要污染用户全局 Python。
- **引文校验失败**：回原书那一页逐字比对。OCR/提取可能有个别字差异——
  以原书为准改总结，而不是把引文删掉了事。
- **一本书跨两个类别**：只放一个主类别（避免两份副本日后不同步），
  其余领域写进总结的「跨领域标签」字段，见 `references/taxonomy.md`。
- **用户中途说"别联网了"**：跳过第 5 步，联网补充一节写「本总结未使用联网检索内容」。
- **书有 800 页以上**：预算必然吃紧。优先保证目录/前言/绪论/结语 100% 读完，
  正文抽样，并在覆盖度里明确说明哪些部分没读、本总结回答不了什么问题。

## 为什么这么做

传统做法是让模型"读一遍书然后写总结"，产出看着漂亮但无法验证，
而且模型会用训练时的常识悄悄替换原书观点。这里换了个思路：

- **把机械判断交给脚本**（格式、文字层质量、章节页范围、页码偏移、引文比对），
  模型只负责它真正擅长的阅读与归纳；
- **让每一条结论都可回查**（页锚 + 可机械核对的引文），
  于是"编造"从"看不出来"变成"跑一次脚本就暴露"；
- **保留目录结构而不是切碎成 chunk**，并生成分层索引当知识地图
  （借鉴 deep-rag 的思路：先看地图再按需下钻，比盲目相似度检索更准）；
- **区分三类出处**（原书文字 / 页面视觉判读 / 联网补充），
  因为它们的可信度不同，混在一起就再也分不开了。

这样产出的知识库，既能让人读懂，也能让后续的检索型 skill 直接用。
